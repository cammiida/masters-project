CONFIG_NAME: 'STREAM'

OUTPUT_PATH: '../output/STREAM/new'


TRAIN:
  FLAG: TRUE
  MAX_EPOCH: 4
  BATCH_SIZE: 32
  RNN_GRAD_CLIP: 5.
  DECODER_LR: 0.0004
  CAP_CNN: '' # No pretrained model
  CAP_RNN: '' # No pretrained model
  STREAM:
    HIDDEN_SIZE: 512
    USE_ORIGINAL: False
    NUM_LAYERS: 1

TEXT:
  EMBEDDING_DIM: 256

