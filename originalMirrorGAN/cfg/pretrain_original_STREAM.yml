CONFIG_NAME: 'STREAM_original'
DATASET_NAME: 'coco'

DATA_DIR: '../data/'
OUTPUT_DIR: '../output/'
GPU_ID: -1
WORKERS: 1

TRAIN:
  FLAG: TRUE
  MAX_EPOCH: 4
  BATCH_SIZE: 32
  RNN_GRAD_CLIP: 5.
  DECODER_LR: 0.0004
  CAP_CNN: '' # No pretrained model
  CAP_RNN: '' # No pretrained model
  STREAM:
    HIDDEN_SIZE: 512
    USE_ORIGINAL: True
    NUM_LAYERS: 1

TEXT:
  EMBEDDING_DIM: 256

